<div class="container">
    <h1>Sistema de Votación de la Nación</h1>
    
    <div class="success-card">
        <h2>Voto Registrado</h2>
        
        <div class="success-icon">
            <svg width="100" height="100" viewBox="0 0 100 100">
                <circle cx="50" cy="50" r="45" fill="none" stroke="#4CAF50" stroke-width="5"/>
                <path d="M30 50 L45 65 L70 35" fill="none" stroke="#4CAF50" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </div>

        <button class="results-button" onclick="window.location.href='/resultados'">Ver Resultados</button>
    </div>
</div>

<style>
    .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 2rem;
    }

    .success-card {
        background: white;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
        text-align: center;
    }

    h1 {
        text-align: center;
        margin-bottom: 2rem;
    }

    h2 {
        margin-bottom: 2rem;
    }

    .success-icon {
        margin: 2rem 0;
        animation: scale-in 0.5s ease-out;
    }

    @keyframes scale-in {
        0% {
            transform: scale(0);
        }
        100% {
            transform: scale(1);
        }
    }

    .results-button {
        padding: 0.75rem 2rem;
        background-color: #4A90E2;
        color: white;
        border: none;
        border-radius: 2rem;
        font-size: 1rem;
        cursor: pointer;
    }

    .results-button:hover {
        background-color: #357ABD;
    }
</style> 